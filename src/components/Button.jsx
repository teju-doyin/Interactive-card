import React from 'react'

const Button = ({text}) => {
  return (
    <button type="submit" className='mt-8 py-4 w-full rounded-md text-white bg-veryDarkViolet'>{text}</button>

  )
}

export default Button